name: CI

permissions:
  contents: write

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  check:
    name: Check (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate OpenAPI clients
        run: pnpm --filter @workspace/api-spec run codegen

      - name: Commit generated clients
        if: matrix.os == 'ubuntu-latest'
        uses: stefanzweifel/git-auto-commit-action@v6
        with:
          commit_message: Regenerate OpenAPI clients
          file_pattern: lib/api-client-react/src/generated lib/api-zod/src/generated

      - name: Verify generated clients are committed
        run: git diff --exit-code -- lib/api-client-react/src/generated lib/api-zod/src/generated

      - name: Check
        run: pnpm run check
